# Compile and run

Compile the code:

```
mix deps.get
mix compile
```

Then, run iex:

```
iex -S mix
```

To start the service, run the following command from the commandline:

```
APNS_Listener.start_link
```
